# 开发进度

> 更新：2026-08-30 · 对应概念设计 v0.5 · 代码基线：git `babbb5e`
> 新会话接手：先读本文件与 `概念设计.md`，源码结构见 README。

## 已完成

- **M0 骨架** ✅：Electron + electron-vite + React + TS + Zustand + SVG 渲染，工程保存/打开
- **启动流程** ✅：启动默认**未打开工程**（欢迎页：新建工程/打开工程入口）；新建/打开后进入编辑器；文件菜单"关闭工程"回到欢迎页；新建/打开/关闭前对未保存修改弹确认
- **M1 绘图 MVP** ✅：
  - 内置图元控件：矩形/圆角矩形/椭圆/线段/占位图、文本、按钮/复选框/进度条/输入框（纯白底黑线）
  - 画布：拖入/点击添加、选择/Shift 多选/全选、移动、8 向缩放手柄、网格显示与吸附、滚轮缩放平移、适配窗口
  - 页面管理（缩略图/复制/删除/重命名）、图层树（显隐/锁定/移层/置顶底/重命名）、属性面板（逐控件属性）
  - 撤销重做（快照栈，拖拽合并一步）、复制/粘贴/再制、对齐分布、快捷键全套、导出 PNG（2x 白底）
- **公共层** ✅（设计 §2/§9/§12-6）：工程级共享层——顶部状态栏等常驻 UI 一次编辑、所有页面共享；渲染于页面内容之下（页面可覆盖）；页面列表顶部"公共层"固定条目（`currentPageIndex === -1`），普通页面中只读不可选（pointer-events 穿透）；页面缩略图与 PNG 导出包含公共层；旧工程文件打开时自动补空公共层
- **M2 之 Tab 页签容器** ✅（设计 §4.1/§9）：
  - 默认 3 页签；页签栏上/下；每页签独立子树（`node.pages`，子控件页面绝对坐标）
  - 画布点击页签头切换当前编辑页；控件拖到内容区自动成为该页签子控件（几何落点判定，在 store.addWidget 内）
  - 移动/删除/复制 Tab 子控件联动；子控件裁剪显示；图层树缩进；缩略图与 PNG 导出递归渲染
  - 属性面板：页签列表（每行一个，增删改名保留已有内容）、栏位置、当前页签

## 待开发（按优先级）

1. **M2 剩余**：
   - 定制控件：创建/发布/实例化/暴露属性/插槽、"存为定制控件"、以容器为骨架新建（概念设计 §5.1–5.5）
   - 锚点系统 + 多分辨率预览（§6）
   - 其余内置容器：面板/弹窗/滚动区/列表/网格/九宫格（当前仅 Tab；子树机制已就绪，照 Tab 模式扩展）
   - 筛选器控件（§4.2，"标记 + 绑定过滤"）
   - Tab 页签的导航型用法 `goto` 绑定页面（§4.1，依赖 M3 的页面跳转）
2. **M3 交互原型**：热点、页面跳转、预览窗口、HTML 导出、流程总览（§8）
3. **M4 导出闭环**：JSON schema 固化、LayaAir 导出器（§10，形态见 §12 暂缓项）

## 已知限制（原型期有意简化）

- 粘贴的控件总落到页面根，不能粘进 Tab 页签内（待重定父/插槽机制）
- 图层树只显示当前页签的子控件，非当前页不可见
- `.uiw` 为 JSON 明文（设计定的是 zip 包，§9）
- 撤销重做为快照栈（设计写命令模式；切页签 activeTab 不入撤销栈）
- 无自动保存/本地历史版本；关闭窗口仍不提醒未保存（仅切换工程时确认）
- 无 native 应用菜单（用应用内菜单栏替代）

## 环境注意事项

- **Electron 二进制下载**需镜像：`ELECTRON_MIRROR="https://npmmirror.com/mirrors/electron/" node node_modules/electron/install.js`
- **@vitejs/plugin-react 必须用 5.x**（6.x 要求 vite 8，与 electron-vite 5 的 peer 范围 ^5||^6||^7 冲突）

## 测试

- 类型检查：`npm run typecheck`
- CDP 自动化（应用运行时）：
  1. `UIW_DEBUG_PORT=9222 npm run dev`
  2. `node tests/cdp-smoke.mjs`（基础功能 21 项）/ `node tests/cdp-tab.mjs`（Tab 容器 15 项）/ `node tests/cdp-common.mjs`（公共层 14 项）
  3. `node tests/demo-tabview.mjs` 在画布上搭 3 页签示例
- 坑：CDP 模拟 Shift 点击需在 `Input.dispatchMouseEvent` 传 `modifiers: 8`
- 截图验证需授予 ZCode 屏幕录制权限（当前未授予，故用 CDP 黑盒测试替代）

## 架构速览（新会话接手要点）

- **单一状态源**：`src/renderer/src/store/editorStore.ts`——文档快照式 undo/redo；所有节点操作递归走子树（walkNodes / collectWithDescendants / findContainerArray）
- **控件渲染单源**：`src/renderer/src/widgets/registry.ts`——`widgetInnerSVG`（外形）/ `renderTreeSVG`（含 Tab 子树，缩略图与导出用）/ `tabContentRect` / `activeTabIndex`；画布、缩略图、PNG 导出共用
- **画布**：`src/renderer/src/components/Canvas.tsx`——递归 `NodeGroup` 渲染；页签头点击命中；容器拖动快照含子孙
- **主进程 IPC**：`project:save` / `project:open` / `png:export`（`src/main/index.ts`）
- **开发测试钩子**：DEV 模式暴露 `window.__uiw`（store）与 `window.__uiwDefs`（控件定义），见 `main.tsx`
- 交互原型（M3）实现时：预览复用同一套 SVG 渲染器（§10）

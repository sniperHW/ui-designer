# 开发进度

> 更新：2026-09-03 · 对应概念设计 v0.5（§8 增补点击交互、编辑器内预览与轻提示） · 代码基线：M2 完成 + 点击交互 + 原型预览 + 轻提示（M3）
> 新会话接手：先读本文件与 `概念设计.md`，源码结构见 README。

## 已完成

- **M0 骨架** ✅：Electron + electron-vite + React + TS + Zustand + SVG 渲染，工程保存/打开
- **启动流程** ✅：启动默认**未打开工程**（欢迎页：新建工程/打开工程入口）；新建/打开后进入编辑器；文件菜单"关闭工程"回到欢迎页；新建/打开/关闭前对未保存修改弹确认
- **M1 绘图 MVP** ✅：
  - 内置图元控件：矩形/圆角矩形/椭圆/线段/占位图、文本、按钮/复选框/进度条/输入框（纯白底黑线）
  - 画布：拖入/点击添加、选择/Shift 多选/全选、移动、8 向缩放手柄、网格显示与吸附、滚轮缩放平移、适配窗口
  - 页面管理（缩略图/复制/删除/重命名——页面与弹窗行均带 **✎ 重命名按钮**，双击亦可）、图层树（显隐/锁定/移层/置顶底/重命名）、属性面板（逐控件属性）
  - 撤销重做（快照栈，拖拽合并一步）、复制/粘贴/再制、对齐分布、快捷键全套、导出 PNG（2x 白底）
- **公共层** ✅（设计 §2/§9/§12-6）：工程级共享层——顶部状态栏等常驻 UI 一次编辑、所有页面共享；渲染于页面内容之下（页面可覆盖）；页面列表顶部"公共层"固定条目（`currentPageIndex === -1`），普通页面中只读不可选（pointer-events 穿透）；页面缩略图与 PNG 导出包含公共层；旧工程文件打开时自动补空公共层
- **M2 控件系统** ✅（设计 §4/§5/§6）：
  - **Tab 页签容器**（§4.1）：默认 3 页签；页签栏上/下；每页签独立子树（`node.pages`）；画布点击页签头切换当前编辑页；属性面板页签列表/栏位置/当前页签，**页签列表编辑按名字对齐内容页**（`remapTabPages` 同名 LCS 匹配：中间插入/删除/调序不错位、改名按序继承内容，激活页签尽量跟随原名）；**页签栏大小可调**——`props.barHeight` 栏高（默认 40，上限为控件高一半，`tabBarHeight()` 统一供渲染/内容区/命中计算）与 `props.fontSize` 页签字号（默认 22），属性面板数值输入或选中后拖页签栏与内容区分界手柄（`SelectionOverlay` 的 `.bar-handle`，吸附网格、显示「页签栏高 N」提示）；两者也已加入可绑定暴露属性键（定制 TabView 实例可调）
  - **其余内置容器**（§4）：面板/弹窗（标题栏+关闭✕）/滚动区（滚动条示意）/列表（纵横、项数）/网格（行列）；拖到容器内容区自动挂子控件（`findDropTarget` 递归最深命中，支持嵌套与插槽）；子控件裁剪、图层缩进、移动联动（`updateNodes` 统一处理容器位移带子孙）
  - **图元补充**：九宫格占位图（三分线）；**筛选器**（§4.2：标签行单选黑底高亮；options/selected；绑定同页列表/网格 + 标记键 tagKey；列表/网格 `itemTags` 每项标记；预览点选过滤随 M3 预览实现）
  - **定制控件全机制**（§5.1–5.5）：
    - 创建：菜单"控件"空白新建 / 以容器为骨架（Tab 可设页签数与栏位）；画布选中组合"存为定制控件"→ 归一化坐标入库并**原位替换为实例**
    - 编辑：`editingWidgetId` 切入定义编辑（画布顶部编辑目标栏 chips 切换，可与页面编辑并存）；改定义保存后**所有实例同步**；实例已覆盖的暴露属性保留
    - 发布设置：名称/分组/默认尺寸/建议锚点（顶部拉伸/底部拉伸——实例化拖到页面根时自动贴边 + 设锚点）
    - 暴露属性：`CustomPropDef`（string/number/boolean/tab-index + 绑定列表 `binds: [{nodeId, key}]`，key 支持 text/fontSize/radius/progress/checked/placeholder/title/selected/activeTab）；实例只覆盖暴露属性（`node.overrides`）
    - 插槽：Tab 每页签默认一槽（键 `${容器id}:${页签}`）；面板/弹窗/滚动区可"开放为插槽"；实例插槽内容存实例（`node.slots`，页面绝对坐标、移动实例跟随）；打散时插槽子控件上提并入对应容器
    - 打散实例为普通组合（解析暴露属性值 + 缩放到实例矩形 + 插槽回填）；嵌套实例保留（只打散一层）
    - 禁止循环引用（自身 + 传递引用链，实例化时拦截）；删除定义前统计影响范围确认，已删实例渲染占位框
  - **锚点 + 多分辨率预览**（§6）：九宫格锚点 × 尺寸模式（固定/随父拉伸/等比；无锚点=随父等比）；状态栏/视图菜单切换 16:9、18:9、19.5:9、4:3 只读预览（`transformTree` 按锚点规则递归重排，容器按内容区映射）；安全区参考框（视图开关）
- **文档结构**：`doc.customWidgets`（定义库）+ `doc.popups`（弹窗页）；旧工程打开自动补 `customWidgets: []`，并把老格式弹窗动作迁移到独立弹窗页
- **原型预览（M3 首版）** ✅（2026-09-01，§8/§10）：
  - 入口：工具栏「▶ 预览」/ 视图菜单「▶ 运行原型预览」；全屏浮层运行整套工程（复用同一套 SVG 渲染器），从当前页起跑，**Esc / ✕ 退出**（退出后停在最后浏览的页面）
  - 预览中可操作：**可点击控件**（按钮 / 自定义开启，含**弹窗浮层内控件**；定制控件实例经定义内命中触发）触发完整效果——切换页面 / 返回上一页（无来路无效）/ 弹出弹窗（遮罩置顶 + **✕ 可点关闭**，与编辑器演示共用 `PopupLayer`）；**定制实例内部静态结构上的可点击控件按命中区域触发**（`pickPathInTree` 几何拾取：实例矩形按比例换算回定义局部坐标 → 从上层到下层命中（容器按内容区裁剪、嵌套实例递归定义树）→ 沿「命中节点 → 祖先」链找第一个可点击控件触发——**可点击标记统一配在定义内，实例自身无标记**）；**Tab 页签头点击就地切换**内容页；**滚动区滚轮滚动**（`PreviewNode` 按内容包围盒计算可滚范围，滑块随偏移动态绘制；嵌套滚动区到边界后放行事件给外层＝滚动链；会话内 `previewScrollOffsets` 记忆各滚动区位置，进入预览时清空）；公共层常驻且**可交互**（如顶栏按钮）；可点击控件悬停显示手型
  - 会话态 `store.previewing`（不入文档）；切工程重置；页面按设计尺寸自适应缩放居中
- **点击交互（M3 首片，§8）** ✅（2026-09-01）：
  - **两类可点击控件**：`button` 天生可点击；其它任意**单个控件**与**弹窗页内控件**（如弹窗里的「确定」）属性面板「点击」区勾选「可点击」（`node.clickable`）开启；**定制控件的可点击标记统一配在定义内的控件上**（定义级，改定义全局生效，预览 / 演示中点击实例对应区域触发）——**实例被单独选中时属性面板不显示「点击」区**，实例自身 `clickable`/`clickAction` 不生效（`isClickable` 对 `custom` 恒 false；预览移除实例级回退；skill 校验器对实例上的 clickable 给 ⚠）；定义编辑内「点击」区带作用域提示，`TypeProps`（文字 / 字号等类型属性）也已对定义编辑开放（此前只能靠暴露属性绑定）
  - **点击效果**（`node.clickAction`）：切换页面（`goto` → 目标页面 id，下拉选择）、返回上一页（`back` → 无需目标，触发时回到跳转来路页面；无来路 / 来路页已删时无效果，会话态 `store.prevPageId` 随切页维护）或弹出弹窗（`popup` → **弹窗页** id，下拉选择）；目标被删时演示给提示
  - **弹窗页（独立设计）**：`doc.popups` 弹窗页数组，页面列表底部「弹窗」分组管理（新建自带居中 dialog / 重命名 / 删除，缩略图预览）；**弹窗本体（首个根级 dialog）标题栏与弹窗页名双向同步**——新建时标题栏即用页名（此前写死「弹窗标题」），重命名弹窗页（✎ / 双击）同步标题栏与图层名，改本体「标题」属性反向重命名弹窗页（列表 / 点击效果下拉 / 弹出显示处处一致，`renamePopup` 统一处理）；`editingPopupId` 第三编辑目标（画布 / 编辑目标栏 / 图层树 / 属性面板 / **选中高亮与 8 向缩放手柄（SelectionOverlay）** / 状态栏 / PNG 导出适配）；**弹窗控件只允许在弹窗页**——普通页面 / 公共层 / **定制控件定义内一律拦截**（addWidget 提示；「以弹窗为骨架新建」入口已移除；含弹窗的组合「存为定制控件」同样拦截）；点击效果「弹窗」旁提供「✏ 编辑弹窗内容」快捷入口直达弹窗页；**弹窗本体（弹窗页根级 dialog）不可删除**——右键菜单不出现删除项、Delete/deleteSelected 自动跳过（删除整个弹窗走弹窗列表 ✕），弹窗内容子控件不受限
  - **弹出显示**：触发后遮罩压暗整页 + 弹窗页内容置顶浮层（`PopupLayer` 以 `PreviewNode` 交互式渲染——**浮层内可点击控件（如确定按钮）可直接左键触发效果**，画布编辑态演示与预览行为一致）；**弹窗标题栏 ✕ 可点击关闭**（与 ✕ 绘制几何对齐的热区），点遮罩 / Esc 也可关闭（会话态 `store.popupId` 不入文档；切页 / 撤销 / 切编辑目标自动收起）
  - **右键菜单**：画布选中控件后右键 →「删除」+「点击」（仅可点击控件显示；未配效果 / 返回无来路 / 目标被删时禁用并列出效果摘要）；点菜单外 / Esc 收起
  - **旧工程迁移**：`loadProject` 自动补 `popups: []`；老格式 popup 动作指向页面上的 dialog 节点 → 自动把该 dialog 移入新建弹窗页并改指向
  - **可见性**：图层树可点击控件显示「点击」标记（`.clickable-tag`）
  - 数据随 `.uiw` 保存；`skills/uiw-designer` 校验器支持 `clickable`/`clickAction`/`popups`（goto→页面 id、popup→弹窗页 id 交叉校验）
  - 顺带修复：删除页面后 `currentPageIndex` 用旧页数收拢导致越界一格（显示层被 clamp 掩盖，原始下标越界）
- **轻提示（M3，§8）** ✅（2026-09-03）：
  - **新控件类型 `tooltip`（轻提示框）**：气泡（圆角矩形）+ 尾箭头（`props.tail`：top/bottom/left/right，默认 bottom 指向被悬停控件）；内容区 = 扣除尾箭头一侧的气泡矩形（子控件裁剪）；默认 280×140
  - **轻提示页（独立设计，添加方式与弹窗一致）**：`doc.tips` 页数组；页面列表「💬 轻提示」分组（新建自带居中轻提示框 / ✎ 双击重命名 / ✕ 删除，缩略图预览）；`editingTipId` 第四编辑目标（画布 / 编辑目标栏 / 图层树 / 属性面板 / 状态栏 / PNG 导出适配）；**轻提示框只允许在轻提示页**——普通页面 / 公共层 / 弹窗页 / 定制控件定义内一律拦截（addWidget 提示；含轻提示框的组合「存为定制控件」拦截）；**本体（首个根级 tooltip）不可删除**（删除整个轻提示走列表 ✕）；重命名页同步本体图层名
  - **轻提示标记（`node.tipTarget` → 轻提示页 id）**：任意单个控件（含弹窗页内控件）属性面板「轻提示」区勾选 + 下拉选择 + 「✏ 编辑轻提示内容」+ 「▶ 演示轻提示」（画布弹出，点击画布 / Esc 关闭）；**定制控件的轻提示标记统一配在定义内控件上**（定义级，实例属性不显示「轻提示」区——与可点击同规则）；图层树显示「轻提示」标记
  - **悬停弹出 / 移开关闭（预览）**：`TipLayer` 无遮罩、锚定触发控件矩形就近浮层（上方优先、空间不足翻到下方并自动上下翻转尾箭头、水平居中收进画板），画布演示共用；普通控件 onMouseEnter/Leave 触发；定制实例 onMouseMove 经 `pickPathInTree`（泛化 match 谓词）命中定义内标记控件，锚定命中区域换算回实例的矩形（含滚动位移还原）；浮层 pointer-events 关闭（移入提示本体不视为离开触发控件），滚轮时收起
  - 删除轻提示页时**清理所有引用该页的 tipTarget**（页面 / 公共层 / 弹窗页 / 轻提示页 / 定制控件定义树）；旧工程打开自动补 `tips: []`；会话态 `store.tip`（切页 / 撤销 / 切编辑目标 / 退出预览自动收起）；`findNodeInDoc` 覆盖轻提示页（提示内容里的可点击控件可触发）
  - skill 同步：schema / widgets / 校验器支持 `tooltip`、`tail`、`tipTarget`（交叉校验 → tips 页 id）与 `doc.tips`；对越位轻提示框与实例上 tipTarget 给 ⚠

## 待开发（按优先级）

1. **M3 交互原型**（§8）剩余项：独立预览窗口（当前为编辑器内嵌全屏预览，后续可拆独立窗口 / 手机外框皮肤）、HTML 导出、热点、筛选器点选过滤、开关/复选框/下拉预览态交互、流程总览（后置）
2. **M4 导出闭环**：JSON schema 固化、LayaAir 导出器（§10，形态见 §12 暂缓项）
3. 官方定制控件模板（§5.6：血条/技能栏/商城卡片等，可用现有"存为定制控件"机制逐个补）

## 已知限制（原型期有意简化）

- 粘贴的控件总落到编辑目标根，不能粘进容器/页签/插槽内（待重定父机制）
- 图层树只显示当前页签/容器的可见子控件层级中"当前页签"的 Tab 子控件；定制实例内部结构不在图层树（只有插槽内容显示）
- 列表/网格的项是生成的统一占位格（可设项数/列数/每项标记），不能往单个格子里放自定义内容（需要时应改用网格容器 + 手摆）
- 分辨率预览为只读模式（不可编辑/选择）；PNG 导出仍按设计尺寸（预览比例不参与导出）
- 定制控件实例缩放时内部结构整体拉伸（字号按两轴均值缩放），插槽子控件不随实例缩放（页面绝对坐标）
- 打散只展开一层，嵌套的其它定制实例保持为实例
- `.uiw` 为 JSON 明文（设计定的是 zip 包，§9）；无自动保存/本地历史版本；关闭窗口仍不提醒未保存（仅切换工程时确认）；无 native 应用菜单（用应用内菜单栏替代）
- 撤销重做为快照栈（设计写命令模式；切页签 activeTab / 切编辑目标不入撤销栈）

## 环境注意事项

- **Electron 二进制下载**需镜像：`ELECTRON_MIRROR="https://npmmirror.com/mirrors/electron/" node node_modules/electron/install.js`
- **@vitejs/plugin-react 必须用 5.x**（6.x 要求 vite 8，与 electron-vite 5 的 peer 范围 ^5||^6||^7 冲突）

## 测试

- 类型检查：`npm run typecheck`
- CDP 自动化（应用运行时）：
  1. `UIW_DEBUG_PORT=9222 npm run dev`
  2. `node tests/cdp-smoke.mjs`（基础功能 22 项：含页面 / 弹窗 ✎ 重命名按钮 + 弹窗页名与标题栏同步）/ `node tests/cdp-tab.mjs`（Tab 容器 19 项：含页签栏高 / 字号属性 + 分界拖拽手柄 + 自定义栏高下页签头命中）/ `node tests/cdp-common.mjs`（公共层 14 项）/ `node tests/cdp-m2.mjs`（M2：容器/锚点预览/定制控件全链路 23 项）/ `node tests/cdp-click.mjs`（点击交互 48 项：可点击开关 / goto / 返回上一页 / 独立弹窗页 / **弹窗标题 ↔ 页名双向同步** / 弹窗选中高亮与伸缩 / 弹窗本体删除保护 / 弹窗禁入页面与定制控件 / ✕ 关闭 / 弹窗页内可配可演示 / 弹窗浮层内控件可点 / 定制定义内可配可演示 / **定制实例属性无「点击」区 + 右键仅删除（实例级标记已移除）** / 旧格式迁移 / 右键菜单）/ `node tests/cdp-preview.mjs`（原型预览 15 项：进入退出 / 跳转 / 返回 / Tab 切换 / 定制实例点击跳页（定义级命中）/ 滚动区滚轮滚动与滑块随动 / 滚出内容可点击 / 弹窗弹出 ✕ 关闭与弹窗内控件可点 / 实例命中区域触发定义级点击与空白区不触发）
  - `node tests/cdp-tip.mjs`（轻提示 15 项：新建轻提示页与本体 / 编辑目标全链路 / 本体删除保护 / 普通页拦截 / 属性区显隐（实例无）/ ▶ 演示与 Esc 关闭 / 页名本体名同步 / 图层树标记 / 预览悬停弹出与移开关闭 / 顶部下方翻转 / 定制实例命中触发与空白不触发 / 删除轻提示引用清理）
  3. `node tests/demo-tabview.mjs` 在画布上搭 3 页签示例
- 坑：CDP 模拟 Shift 点击需在 `Input.dispatchMouseEvent` 传 `modifiers: 8`；右键点击传 `button: 'right', buttons: 2`；控件库条目在可视区外时先 `scrollIntoView` 再点击；测试里先覆写 `window.alert/confirm` 防阻塞
- 截图验证需授予 ZCode 屏幕录制权限（当前未授予，故用 CDP 黑盒测试替代）

## 架构速览（新会话接手要点）

- **单一状态源**：`src/renderer/src/store/editorStore.ts`——文档快照式 undo/redo；编辑目标抽象 `editRoot()/editNodesOf()`（页面 / 公共层 / 定制控件定义树统一一套操作）；`updateNodes` 检测位移增量并让容器子孙跟随；落点 `findDropTarget` 递归命中最深容器/插槽；定制控件动作（createCustomWidget / saveSelectionAsCustom / publishCustomWidget / detachInstance / mutateWidget …）
- **控件渲染单源**：`src/renderer/src/widgets/registry.ts`——`widgetInnerSVG`（外形）/ `renderTreeSVG(n, defs)`（含容器子树、定制实例、插槽，缩略图与导出用）/ `renderCustomInstance`（解析暴露属性 → scaleTree 缩放 → 渲染）/ `slotsOfDef` / `contentRectOf` / `pickPathInTree`（预览点击拾取：容器内容区裁剪 + 定制实例递归定义树）/ `transformTree`+`transformRect`（锚点重排）/ `previewDims`；画布、缩略图、PNG 导出共用
- **树遍历**：`src/renderer/src/widgets/tree.ts`——walkNodes / findNodeById / findContainerArray / collectWithDescendants / bboxOf / reids（克隆换 id），覆盖 pages/children/slots
- **画布**：`src/renderer/src/components/Canvas.tsx`——递归 `NodeGroup`（容器子控件裁剪、实例插槽交互、内部结构只读）；页签头点击命中；分辨率预览只读态；`EditTargetBar`（页面/公共层/弹窗页/定制定义 chips）；右键菜单 `CanvasCtxMenu`（删除/点击，弹窗本体无删除项）；弹窗弹出层抽为 `PopupLayer.tsx`（`PreviewNode` 交互式渲染，画布演示与预览共用）
- **原型预览**：`src/renderer/src/components/Preview.tsx`（全屏浮层 + 自适应缩放）+ `PreviewNode.tsx`（预览态节点递归渲染：可点击控件 / Tab 页签交互 / 滚动区滚轮滚动与动态滑块，画布弹窗演示经 `PopupLayer` 复用），复用 registry 渲染器；`tree.ts` 的 `findNodeInDoc` 覆盖公共层 / 页面 / 弹窗页 / 定制控件定义（`triggerClick` 全文档解析）
- **主进程 IPC**：`project:save` / `project:open` / `png:export`（`src/main/index.ts`）
- **开发测试钩子**：DEV 模式暴露 `window.__uiw`（store）与 `window.__uiwDefs`（控件定义），见 `main.tsx`
- 交互原型（M3）实现时：预览复用同一套 SVG 渲染器（§10）；筛选器过滤、Tab goto、**可点击控件（clickable/clickAction）**的数据与编辑器内触发演示（`store.triggerClick`）已就位

# v5 绿色卡：单一徽章底座与默认状态清理

- 左上种族徽章只有现有的 `配型底 + 配型图标` 两个节点拥有视觉底座；卡框左上恢复为与右上对称的普通卡角，不再叠加第二个装饰座。
- `可升级标识` 在默认未升级状态以节点 `visible: false` 隐藏；升级状态应由交互/数据显式打开，不使用白点或空文本占位。
- 卡面主框、卡面无内框、真实 alpha 与孤立近白残留检查沿用 v4 门禁。

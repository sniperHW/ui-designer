# v3 绿色卡：结构重建验收

本版本保留 v2，不覆盖任何已验收文件。它按风格锚点中的卡牌信息结构重建：

- 卡面窗：`16,24,128,131`，为卡牌主视觉区；立绘以全覆盖、不透明资源绑定。
- 等级槽：`16,162,128,28`，运行时等级文字在独立容器内显示。
- 进度槽：`16,196,128,16`，作为最小的三级信息区。
- 左上种族徽章仍为上层节点，不会由卡面裁切。
- 外框使用最终设计像素的 `15px` 抗锯齿切角透明轮廓；生成候选遗留在四角的展示底不允许进入 `.uiw`。

所有文本和数值均保持 `.uiw` 运行时节点，卡框素材不烘焙文字、角色或数值。

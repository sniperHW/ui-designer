# v4 绿色卡：无内框、无残留像素验收

v4 不覆盖 v2 或 v3。它把角色立绘窗扩大到卡牌主框内缘
`8,18,144,142`，并将原先的内侧金属立绘框纳入透明孔，因此立绘直接贴合
主卡框；等级槽和进度槽保持独立。

卡框候选额外执行两项门禁：

- 外框为抗锯齿切角 alpha 轮廓；四角不存在展示底。
- 所有中性近白残留必须先定位，再用 `neutral_clear_rects` 定向清除。
  当前候选检测到的孤立白点为 `147,3`，已列入清除区。

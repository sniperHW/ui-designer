# 顶栏资源组 v3：分层素材

资源条在原有 `153×58` 节点规格中重构为：无文字、无左侧图标槽的通用资源条底板、
四个独立 `32×32` 透明资源图标、UIW 运行时数值。底板不含资源种类或数值；图标不含
白底、棋盘展示底或文字。底板不得以纯色方块覆盖旧图标，也不得制造圆形、六边形、
矩形或徽章式图标底座；透明图标直接叠加在完整的资源框内衬上。底板会清除从画布外沿
连通的近黑展示哑光，并以抗锯齿切角 alpha 轮廓封边。右侧功能位维持既有页面结构，
不改变资源数值逻辑。

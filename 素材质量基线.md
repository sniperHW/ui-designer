# UI 素材质量基线 v1.1

## 定位与适用范围

本文件是本仓库所有 UI 素材的全局质量基线，适用于导入 `.uiw`、H5 原型或 LayaAir 的任何 PNG 及其派生版本。它独立于具体页面、阶段和累计预览版本；卡牌页的《素材质量标准.md》是本基线在卡牌场景的既有实施记录。

页面或组件可以增加更严格的限制，但不得降低本基线。发生冲突时，以更严格者为准。

## 素材职责

- **容器 / 框体**：只承担完整框体、内衬和固定分隔线；不得烘焙文字、数值、进度、选中态或功能图标。
- **图标**：必须是无底图的真实透明素材，直接叠加到所属容器内衬；同一语义在不同页面或位置复用同一原始轮廓，仅允许尺寸、位置和单色化适配。
- **动态信息**：文字、数值、进度、选中态和可见性均由运行时节点承担。
- **卡牌**：层级固定为卡面图片 → 卡框 → 种族底座 → 种族图标 → 动态等级 / 进度 / 升级标；卡面按窗口精确 `cover`，合成后 alpha 必须全不透明。
- **可变尺寸框体**：优先使用一张无文字的九宫格皮肤，而不是由角、边、底色等多个运行时节点拼装；文字、图标、角标、红点和数值始终由独立运行时节点叠加。

## 九宫格皮肤规范（Laya 手机小游戏）

本节适用于按钮底图、面板、弹窗框、卡框、凹槽等需要覆盖多个尺寸的框体皮肤。它描述通用的九宫格拉伸规则，后续由 Laya 的九宫格组件消费，不依赖 Cocos 或 Android `.9.png` 标记格式。

### 交付形态

- 一个可九宫格拉伸的视觉皮肤交付为**一张**小尺寸 PNG，配套 `sourceSize: [源宽, 源高]` 与 `nineInsets: [左, 上, 右, 下]`；不得把四角、边条和中心作为多个运行时节点拼装。
- 高度对称的常规框体，优先令四个角与四条边在视觉上可镜像 / 旋转复用；但不对带标题结构、定向光照、单侧装饰等非对称框体强行套用对称边距。
- 中心区可为纯色、低频纹理或透明。若透明，必须在素材合同中声明由哪个下层节点提供底色；不得以未声明的黑底或白底填充。
- 除非画面表现确有差异，正常、按下、禁用、选中等状态共用同一基础皮肤并由运行时着色、遮罩或独立角标表达；确需不同美术皮肤时，它们属于同一个状态素材族，仍各自交付完整九宫格皮肤。

### 生图与制图限制

- 四个固定角内的圆角、倒角、铆钉、边缘高光与描边不得被拉伸；上下边只能横向拉伸，左右边只能纵向拉伸，中心才可双向拉伸。
- 切片线必须落在颜色、透明度和描边均连续的安全带中。任何文字、数字、图标、方向性花纹、角标、强高光转折或边框端点不得跨越切片线。
- 中心区不得使用密集、不可拉伸的花纹。边条若含纹样，纹样须在拉伸后仍连续自然；无法保证时改用简单渐变、低频纹理或另行确认的平铺方案。
- 生图提示词和后处理说明必须写明“无文字、无图标、无角标、完整闭合边框、四向对称（如适用）、中心可拉伸、安全切片带”。AI 生图结果不能仅凭外观相似即视为九宫格可用。

### 尺寸、图集与性能

- 源图以目标清晰度下的最小合理尺寸交付，不为每个按钮或面板尺寸重复出图；同一视觉皮肤跨尺寸只保留一份源图和切片数据。
- 目标最小宽度必须不小于 `左 + 右`，目标最小高度必须不小于 `上 + 下`；实际合同还应记录已验证的最小尺寸，避免固定角互相挤压。
- 同一页面或组件族优先复用相同 `assetId`，进入同一职责正确的图集。图集边缘须保留并外扩 2 个源像素的同色边缘像素，防止线性采样时串入相邻素材；不以透明空白替代外扩边。
- 运行时优先由一个九宫格控件使用该单张皮肤。是否能合批仍取决于图集、材质和渲染状态，不能把“九宫格”本身承诺为固定的单次绘制；规范的目标是减少图片数量、节点数量与重复素材。

### 素材合同与验收

每个启用九宫格的素材版本，必须记录以下最小合同；`nineInsets` 与 UIW 的同名字段保持相同顺序：

```json
{
  "resizeMode": "nine-slice",
  "sourceSize": [96, 96],
  "nineInsets": [24, 24, 24, 24],
  "minimumTargetSize": [48, 48],
  "centerFill": "opaque",
  "symmetry": "four-way"
}
```

- `resizeMode` 为 `nine-slice`；不拉伸的素材应明确为 `fixed`，不使用模糊的“可缩放”描述。
- `sourceSize` 与实际 PNG 像素尺寸一致；四个 `nineInsets` 均为非负像素，左右之和必须小于源宽，上下之和必须小于源高。
- `minimumTargetSize` 不得小于固定边距之和；`centerFill` 只能为 `opaque` 或 `transparent`；`symmetry` 使用 `four-way`、`mirrorable` 或 `custom` 描述经确认的结构，不以默认假设替代确认。
- 进入页面装配前，必须在最小尺寸、原生尺寸和该素材实际使用的最大尺寸下检查四角、边条、中心、透明边缘和切片接缝；任一尺寸出现拉伸变形、断线、色差、串图或内容误入拉伸区即不通过。
- 装配到 UIW 时，`assetSrc`、`nineSourceSize`、`nineInsets` 必须一并写入；导入 Laya 时使用相同的四边距。不得在下游重新猜测切片位置。

## 强制质量门禁

1. 所有独立 PNG 必须为 RGBA，具备真实 alpha；不得出现白底、棋盘底、黑边、灰边、预乘色、透明通道残留或未声明的孤立亮点。
2. 异形轮廓必须清除从画布外侧连通的近黑展示底；边界带与深色阈值须按真实结构测量，不能用放宽阈值或像素预算保留展示底。
3. 闭合金属框须检查上、下、左、右及四角；设计要求对称时，边缘亮度、厚度和切角必须对称且连续。
4. 一个语义元素只能有一个视觉底座；禁止为覆盖旧烘焙内容而添加额外的方形、圆形、六边形或徽章座。
5. 复用按钮必须使用完整、闭合的状态皮肤。并列装配必须声明每项尺寸、固定间距和总宽度；重叠双线、溢出、裁断均不允许。
6. 重装配、局部修改或派生尺寸均视为新候选，必须重新经过适用门禁。
7. 脚本门禁通过后，必须在真实设计工具画布的实际背景、100% 缩放和实际缩放下验收；仅查看 PNG 或 JSON 不构成验收。
8. 标记为 `resizeMode: nine-slice` 的素材，必须同时通过本文件“素材合同与验收”中的结构校验与最小 / 原生 / 最大尺寸画布验收。
9. 卡面、头像或其他受框体约束的内容窗口，必须在页面合同的 `assemblyGates` 中记录准确矩形与最小安全边；装配和真实画布验收必须从该合同读取，不得在多个脚本中各自硬编码一份尺寸。

## 全局素材管线的执行要求

- 每个素材以稳定 `assetId` 登记语义、母版、派生版本、质量配置和消费者，不能仅依赖文件名或 UIW 节点名称。
- 页面装配合同只描述“哪个节点使用哪个已验收素材版本”，无权豁免或降低质量门禁。
- 素材构建必须先产出候选版本，再做门禁和画布验收；通过后才可进入页面装配。
- 页面装配必须以当前验收版本为输入生成新的候选 UIW，禁止覆盖既有验收版本或借全量重建丢失手工交互调整。
- 下游 H5 / LayaAir 发现视觉资产问题时，必须回流到本管线创建新候选；不得在下游私改后形成第二份素材真相。
- 用户确认后的页面必须执行合同声明的单一发布门禁命令；该命令必须校验已确认文件哈希、可复现装配结果和真实渲染结果，并采用失败即停止策略。

## 规范文件关系

- `assets/ui-pipeline/quality-profiles/ui-material-v1.json`：可由脚本读取的质量配置标识与门禁集合。
- `assets/ui-pipeline/asset-catalog.json`：全局素材注册表，登记素材、派生版本和消费者。
- `assets/ui-pipeline/page-contracts/`：页面专属装配合同；只允许增加本页限制。
- 本文件“九宫格皮肤规范（Laya 手机小游戏）”：可变尺寸框体的统一生图、切片、图集与验收合同。
- `output/card-page-anchor-reconstruction-v1/素材质量标准.md`：卡牌页现有的细化规则和已验证实施依据；其更严格的条款继续有效。

# UI 素材质量基线 v1

## 定位与适用范围

本文件是本仓库所有 UI 素材的全局质量基线，适用于导入 `.uiw`、H5 原型或 LayaAir 的任何 PNG 及其派生版本。它独立于具体页面、阶段和累计预览版本；卡牌页的《素材质量标准.md》是本基线在卡牌场景的既有实施记录。

页面或组件可以增加更严格的限制，但不得降低本基线。发生冲突时，以更严格者为准。

## 素材职责

- **容器 / 框体**：只承担完整框体、内衬和固定分隔线；不得烘焙文字、数值、进度、选中态或功能图标。
- **图标**：必须是无底图的真实透明素材，直接叠加到所属容器内衬；同一语义在不同页面或位置复用同一原始轮廓，仅允许尺寸、位置和单色化适配。
- **动态信息**：文字、数值、进度、选中态和可见性均由运行时节点承担。
- **卡牌**：层级固定为卡面图片 → 卡框 → 种族底座 → 种族图标 → 动态等级 / 进度 / 升级标；卡面按窗口精确 `cover`，合成后 alpha 必须全不透明。

## 强制质量门禁

1. 所有独立 PNG 必须为 RGBA，具备真实 alpha；不得出现白底、棋盘底、黑边、灰边、预乘色、透明通道残留或未声明的孤立亮点。
2. 异形轮廓必须清除从画布外侧连通的近黑展示底；边界带与深色阈值须按真实结构测量，不能用放宽阈值或像素预算保留展示底。
3. 闭合金属框须检查上、下、左、右及四角；设计要求对称时，边缘亮度、厚度和切角必须对称且连续。
4. 一个语义元素只能有一个视觉底座；禁止为覆盖旧烘焙内容而添加额外的方形、圆形、六边形或徽章座。
5. 复用按钮必须使用完整、闭合的状态皮肤。并列装配必须声明每项尺寸、固定间距和总宽度；重叠双线、溢出、裁断均不允许。
6. 重装配、局部修改或派生尺寸均视为新候选，必须重新经过适用门禁。
7. 脚本门禁通过后，必须在真实设计工具画布的实际背景、100% 缩放和实际缩放下验收；仅查看 PNG 或 JSON 不构成验收。

## 全局素材管线的执行要求

- 每个素材以稳定 `assetId` 登记语义、母版、派生版本、质量配置和消费者，不能仅依赖文件名或 UIW 节点名称。
- 页面装配合同只描述“哪个节点使用哪个已验收素材版本”，无权豁免或降低质量门禁。
- 素材构建必须先产出候选版本，再做门禁和画布验收；通过后才可进入页面装配。
- 页面装配必须以当前验收版本为输入生成新的候选 UIW，禁止覆盖既有验收版本或借全量重建丢失手工交互调整。
- 下游 H5 / LayaAir 发现视觉资产问题时，必须回流到本管线创建新候选；不得在下游私改后形成第二份素材真相。

## 规范文件关系

- `assets/ui-pipeline/quality-profiles/ui-material-v1.json`：可由脚本读取的质量配置标识与门禁集合。
- `assets/ui-pipeline/asset-catalog.json`：全局素材注册表，登记素材、派生版本和消费者。
- `assets/ui-pipeline/page-contracts/`：页面专属装配合同；只允许增加本页限制。
- `output/card-page-anchor-reconstruction-v1/素材质量标准.md`：卡牌页现有的细化规则和已验证实施依据；其更严格的条款继续有效。

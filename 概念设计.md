# 手游 UI 雏形设计工具 · 概念设计

> 版本 v0.5 草案（2026-08-30）
> v0.5 变更：新增"公共层"概念（工程级共享层：顶部状态栏等常驻 UI 在所有页面复用，见 §2 术语与 §12 决议 7）。
> v0.4 变更：依据参考截图（底部功能导航 / 牌组子页签 / 卡牌筛选行）完善设计——新增"存为定制控件"与"以容器为骨架新建"、默认页签 `activeTab`、页签分内容型 / 导航型（`goto`）、页签内容区默认开放为插槽、新内置控件"筛选器"；页签栏方向决议为只做上 / 下。
> v0.3 变更：内置控件分为"图元 / 容器"两类；Tab 页签定为内置容器；定制控件新增插槽机制。
> 跨平台（Windows / macOS）手游 UI **线框图 + 可交互原型**设计工具，基于 Electron。

## 1. 产品定位

- **一句话**：用线框图快速搭建手游界面的骨架与跳转流程，产出可点击演示的原型、评审参考图和可对接引擎的布局数据。
- **不做什么**：不做视觉稿 / 高保真设计（那是 Figma / PS 的事）。专注低保真：布局、层级、信息结构、交互流程。
- **视觉风格**：纯白底黑线的经典线框风，不做灰阶 / 主题色块（已决议，见 §12）。
- **目标用户**：独立游戏开发者、小团队的策划 / UI、需要快速验证界面方案的人。
- **核心卖点**：
  1. **手游向**：横竖屏、多分辨率适配（锚点）、刘海屏安全区、游戏常用控件模板；
  2. **控件组合复用**：定制控件 = 用已有控件拼装 + 暴露参数，改一处定义、全局实例同步更新；
  3. **一份设计、三种产出**：PNG 演示图 / JSON 布局数据 / 引擎 UI 代码（首选对接 LayaAir）。

## 2. 核心概念

| 术语 | 含义 |
|---|---|
| 工程 Project | 一次设计工作的整体：设计尺寸、全部页面、定制控件库、素材 |
| 页面 Page | 一屏 UI（如：主城、背包、结算），是交互原型中的跳转节点 |
| 公共层 Common Layer | 工程级共享层，内容显示在**所有页面**（典型：顶部状态栏、常驻导航）；渲染于页面内容之下，普通页面中只读，在页面列表顶部的"公共层"条目中编辑 |
| 弹窗页 Popup Page | 独立设计的弹窗内容（`doc.popups`，页面列表底部「弹窗」分组管理）；普通页面 / 公共层不放弹窗控件，由点击效果（popup）遮罩置顶弹出，✕ / 遮罩 / Esc 关闭 |
| 图元 Primitive | 内置的最小绘图单元（矩形、文本、占位图……），叶子节点，不可再拆分 |
| 容器控件 Container | 内置的结构类控件（面板、滚动区、Tab 页签……），编辑器原生支持在其内部区域挂载子控件 |
| 定制控件 | 用户用内置控件 / 其它定制控件拼装并发布到控件库的可复用控件 |
| 实例 Instance | 拖入页面的控件副本；定制控件的实例只能覆盖"暴露属性"、向插槽挂子控件，不能修改定义的内部结构 |
| 暴露属性 Prop | 定制控件对外发布的参数（如按钮文字、主题色），内部子控件属性绑定到它 |
| 插槽 Slot | 定制控件定义中预留的命名内容区域；实例可向插槽挂任意子控件，子控件数据属于实例而非定义 |
| 锚点 Anchor | 控件相对父容器边缘的定位 / 拉伸规则，多分辨率适配的基础 |
| 热点 Hotspot | 页面上的透明可点区域，预览时触发页面跳转 |

## 3. 界面布局

```text
+----------------------------------------------------------------------+
| 菜单栏：文件  编辑  视图  控件  页面  预览  帮助                          |
| 工具栏：选择  热点 ｜ 撤销 重做 ｜ 对齐 分布 ｜ 网格 吸附 ｜ 缩放  ▶预览  |
+---------------+--------------------------------------+---------------+
| 控件库         |            UI 设计区（画布）           |  属性面板      |
| ┌───────────┐ |  · 画布 = 当前页面，按设计尺寸渲染      |  · 位置/尺寸   |
| │ ▸ 内置控件 │ |  · 网格 / 标尺 / 参考线 / 智能对齐线    |  · 锚点/适配   |
| │ ▸ 定制控件 │ |  · 选中框 + 8 向缩放手柄               |  · 控件属性    |
| │ (搜索/分组) │ |  · 定制控件编辑时：此区变为控件设计区   |  · 事件(热点)  |
+ ├───────────┤ +--------------------------------------+---------------+
| │ 页面列表   | |  页面缩略图列表：新建 / 复制 / 排序 / 重命名             |
| │ (左下)     | +--------------------------------------+               |
| │ 图层树     | |  当前页面图层树：拖拽排序 / 显隐 / 锁定 / 重命名         |
+ └───────────┘ +--------------------------------------+---------------+
| 状态栏：鼠标坐标 · 缩放比例 · 选中对象信息 · 分辨率预览切换                |
+----------------------------------------------------------------------+
```

菜单栏要点：

- **文件**：新建工程（向导：横/竖屏、设计尺寸、安全区）、打开、保存、自动保存设置、导出（PNG / JSON / 代码）、最近工程
- **编辑**：撤销 / 重做、复制 / 粘贴、偏好设置
- **视图**：缩放、适配窗口、网格 / 标尺 / 参考线开关、显示锚点线
- **控件**：新建定制控件、编辑选中实例的定义、打散实例为普通组合
- **页面**：新建 / 复制 / 删除 / 排序
- **预览**：运行原型（独立窗口）、全屏演示

## 4. 内置控件（图元 + 容器）

内置控件分两类：**图元**是叶子，不可再拆；**容器**是结构类控件，编辑器原生支持在其内部挂子控件。保持最小集，游戏风格控件靠"官方定制控件模板"提供（见 §5.6），两者都可在库中直接使用。

- **形状图元**：矩形、圆角矩形、椭圆、线段、占位图（叉线框）、占位头像
- **文本图元**：单行文本、多行文本、标题样式（预设字号层级）
- **交互图元**：按钮、复选框、单选框、开关、滑条、进度条、输入框、下拉框、筛选器（标签行，点选后按标记过滤绑定的列表 / 网格，见 §4.2）
- **容器控件**：面板、弹窗、滚动区、列表（纵向 / 横向）、网格（行列数可调）、九宫格缩放图元、Tab 页签

> "数量可变的重复结构"（列表项、网格、Tab 的页签）必须是内置容器——编辑器需要原生理解可增删的重复区域，靠拼装表达不出来。用户自己的"内容因实例而异"的结构控件（标题栏固定、内容开放的弹窗等）由定制控件的**插槽**机制实现（§5.5）。

### 4.1 Tab 页签容器

- **页签栏位置**：属性 `barPosition`，只做 **上 / 下**（已决议不做左 / 右，见 §12）；
- **页签头**：数量、文字可增删改、可拖拽排序；
- **默认页签**：属性 `activeTab`，编辑态与预览态的初始选中页签；发布为定制控件后是天然的暴露属性——底部导航拖到每个页面后，覆盖 `activeTab` 让各页高亮自己的页签；
- **每页独立内容**：每个页签是一个独立子容器，各自挂任意子控件——这正是 Tab 必须是容器、不是图元的原因；
- **页签动作（两种用法，同一控件）**：
  - **内容型**：点击页签头就地切换该页签的内容区（如"牌组"界面的 1–5 号卡组页签）；
  - **导航型**：页签绑定目标页面（`goto`），预览中点击即跳转页面（如底部功能导航：商城 / 卡牌 / 战斗……）。导航型页签无需往页签里塞内容；
- **编辑**：选中 Tab 后点击页签头切换"当前编辑页"，非当前页在设计区隐藏；
- **预览**：内容型页签就地切换内容页，导航型页签执行页面跳转，均无需热点（见 §8）。

**示例（用户工作流）**：新建定制控件"主页底部导航"→ 选择"以 Tab 容器为骨架"→ 页签数 5、`barPosition: bottom`、`activeTab: 3` → 为各页签绑定目标页面 → 逐页签编辑图标占位 → 发布 → 拖到每个页面，按页覆盖 `activeTab`。

### 4.2 筛选器

牌组、图鉴类界面的标签筛选行（参考截图中"全部 + 阵营图标 + 已收集计数"那一行）：

- **形态**：一行标签（N 个，文字或小占位图标），单选高亮；`mode` 预留多选扩展；
- **属性**：`options`（标签列表）、`selected`（当前选中项）、`defaultSelected`（默认"全部"或第一项）；
- **过滤绑定**：可绑定同页的一个列表 / 网格控件 + 一个标记键（如 `kind`）；被绑定控件的项目带标记值，预览时点选标签即只显示匹配项目，"全部"显示所有；
- **编辑态**：恒显示全部项目，标签仅表现选中态；导出 PNG 按"当前选中标签 + 全部项目"渲染（与编辑态一致）；
- 归类为**交互图元**（与下拉框同类：选项是生成的，不承载用户子控件）。

## 5. 定制控件机制（核心特性）

### 5.1 创建流程

**入口（三选一）**：

- 菜单"控件 → 新建定制控件"，从空白开始；
- 新建时选择**以内置容器为骨架**（如选 Tab 页签：先设定页签数、栏位置、默认页签，再进入编辑）；
- 画布上选中任意控件 / 组合 → 右键**"存为定制控件"**——最短路径：在页面上搭好、调好后直接入库复用。

**编辑与发布**：

1. 设计区切入**控件编辑模式**（新标签页，可与页面编辑并存）；
2. 用内置控件（图元或容器）/ 已有定制控件拼装内部结构；
3. 声明**暴露属性**：把内部子控件的属性绑定到对外参数上（例：`label: string` → 内部 Text 的 text；`theme: color` → 同时绑定多个图形的 fill；Tab 的 `activeTab` → 实例高亮不同页签）；需要各实例自行填内容的位置，把对应容器内容区标记为**插槽**（§5.5）；
4. 点击**发布**：设置名称、分组、默认尺寸、**建议锚点**（如"底部拉伸"——底部导航 / 顶栏类控件拖入页面时自动吸附对应边）、缩略图（自动截取）→ 出现在左侧控件库"定制控件"分组。

### 5.2 使用与更新

- 拖入库中定制控件到页面 = 创建实例；实例只能覆盖暴露属性、向插槽（若有）挂子控件；
- 想单独改某个实例的内部结构 → **打散**成普通组合（解除与定义的关联）；
- 编辑定制控件定义并保存 → 所有实例**同步更新**；实例已覆盖的暴露属性值保留；
- 定制控件可以嵌套引用其它定制控件；保存时校验引用关系，**禁止循环引用**。

### 5.3 库管理

分组、搜索、重命名、删除（被页面引用时给出提示与影响范围）。单控件导入导出与团队库共享：暂不考虑（已决议，见 §12）。

### 5.4 适配要求

定制控件内部布局使用锚点 / 百分比，保证被拉伸到不同尺寸时结构正确。

### 5.5 内容插槽（Slot）

只有"暴露属性"的定制控件承载不了**每个实例各不相同的内容**。插槽 = 定制控件内部被**开放**的容器内容区：

- **定义时**：控件内部的容器（面板、滚动区、Tab 页签……）内容区可逐个标记"开放为插槽"，也可在发布时统一勾选；**Tab 的每个页签默认开放**——每个页签即一个具名插槽；
- **使用时**：实例可向插槽挂任意子控件，子控件保存在实例数据上，不属于控件定义；
- **更新定义**：插槽内的实例子控件原样保留；**打散**实例时，插槽子控件上提到页面；
- **边界**：插槽的位置 / 尺寸由定义决定，实例只能往里放东西；需要"数量可变的重复区域"（页签数不同的 Tab、列表项）仍应使用内置容器（§4）——发布前改好结构，或打散后修改。

### 5.6 官方定制控件模板（随软件内置，可视为预置的定制控件）

血条、技能栏、道具格网格、金币 / 资源栏、聊天滚动区、任务追踪条、商城卡片、结算面板、设置弹窗、加载进度页、虚拟摇杆占位、小地图占位、头像框、底部功能导航（Tab 骨架，页签可绑定页面跳转，见 §4.1）。

## 6. 多分辨率适配（手游关键）

- 新建工程时设定**设计尺寸**（预设：1334×750、1280×720、1170×2532 等）与横 / 竖屏；**一个工程锁定一种方向**，不做横竖双方案（已决议，见 §12）；
- 每个控件有**锚点**：简化为九宫格锚点 + 四边距 + 拉伸模式（固定尺寸 / 随父拉伸 / 等比）；
- **安全区**：画布可叠加刘海屏安全区参考框，可作为锚点目标；
- **分辨率预览**：设计区 / 预览模式可一键切换 16:9、18:9、19.5:9、4:3 等常见比例，按锚点规则实时重排，检验适配效果。

## 7. 编辑器核心功能

- **选择**：单击、框选、图层树点选；Shift / Ctrl 多选；
- **变换**：拖动移动、8 向手柄缩放（旋转后置）；方向键微调（±1，Shift ±10）；
- **吸附**：网格（步长可设）、参考线（从标尺拖出）、智能对齐（边缘 / 中心线 / 等间距提示线）；
- **对齐 / 分布**：左右上下对齐、水平 / 垂直居中、等间距分布；
- **层级**：图层树拖拽排序、置顶 / 置底、快捷成组 / 解组；
- **复制**：Ctrl+C/V（含跨页面）、按住 Alt 拖拽复制；
- **撤销 / 重做**：Ctrl+Z / Ctrl+Shift+Z，命令模式实现，覆盖所有文档修改；
- **缩放**：Ctrl+滚轮、100% 按钮、适配窗口。

## 8. 交互原型

- **控件原生交互**（无需热点）：Tab 内容型页签就地切换内容页、导航型页签跳转目标页面（§4.1）、筛选器点选标签即时过滤绑定的列表 / 网格（§4.2）、开关 / 复选框 / 单选框切换状态、下拉框展开选项，预览模式下直接可操作；
- **可点击控件 + 点击效果**（编辑器内已实现触发演示）：`button` 天生可点击，其它任意控件（含定制控件实例）可在属性面板开启「可点击」；点击效果三种——**切换页面**（`clickAction.goto` → 目标页面）、**返回上一页**（`clickAction.back` → 回到跳转来之前的页面，无来路时无效）、**弹出弹窗**（`clickAction.popup` → **弹窗页**：弹窗在独立的弹窗页中设计——普通页面 / 公共层不允许放弹窗控件，页面列表底部「弹窗」分组管理；触发时遮罩压暗、弹窗内容置顶浮层显示，点弹窗 ✕ / 遮罩 / Esc 关闭）。编辑器内**选中控件后右键 →「点击」**即可触发效果演示（预览窗口实现后同样生效）；
- 页面上可放置**热点**（透明区域，编辑态显示虚线框）；热点默认继承目标控件的形状（选中按钮右键"设为热点"更快捷）；
- 热点动作（首期只做第一种，其余作为扩展点）：
  1. 跳转到指定页面（预览带返回，Esc / 右键返回上级）；
  2. （后期）显示 / 隐藏指定面板、弹窗打开 / 关闭；
- **预览**：独立窗口按设计尺寸运行，可选手机外框皮肤，页面间可点击跳转；
- **流程总览**：自动生成页面节点 + 跳转箭头的关系图，便于评审（后置版本）。

## 9. 数据与文件格式

工程保存为单文件 `.uiw`（实际是 zip 包），自动保存 + 本地历史版本。

```jsonc
// project.json（示意）
{
  "meta": { "name": "demo", "version": 1,
            "designWidth": 1334, "designHeight": 750,
            "orientation": "landscape", "safeArea": { "top": 0.03 } },
  "commonLayer": { "id": "common", "name": "公共层",
                   "nodes": [ /* 顶部状态栏等所有页面常驻的控件 */ ] },
  "customWidgets": [ {
      "id": "w.primary_button", "name": "主按钮",
      "size": { "w": 240, "h": 80 },
      "props": [ { "name": "label", "type": "string", "default": "确定" } ],
      "tree": { "type": "rect", "props": { "fill": "#DDD", "radius": 12 },
                "children": [ { "type": "text",
                  "props": { "text": "${label}" } } ] } } ],
  "popups": [ { "id": "pp.confirm", "name": "确认弹窗",
                "nodes": [ /* 独立设计的弹窗内容（典型：居中 dialog），点击效果弹出 */ ] } ],
  "pages": [ {
      "id": "p.main_city", "name": "主城",
      "tree": { "type": "panel", "children": [ /* 控件节点… */ ] },
      "hotspots": [ { "id": "h1", "rect": [10,10,240,80], "action": { "goto": "p.bag" } } ] } ]
}
```

页面内控件节点（示意）：

```jsonc
{ "id": "n1", "type": "w.primary_button", "name": "进入背包",
  "rect": [920, 40, 240, 80],
  "anchor": { "preset": "top-right", "margin": { "right": 174, "top": 40 } },
  "props": { "label": "背包" },
  "clickAction": { "type": "goto", "target": "p.bag" },
  "children": [] }
```

点击效果三种形态（`button` 天生可点击，其它控件加 `"clickable": true` 开启，§8）：

```jsonc
// 切换页面：target = 目标页面 id
{ "clickAction": { "type": "goto", "target": "p.bag" } }
// 返回上一页：无需 target，运行时取跳转来路页面（无来路时无效）
{ "clickAction": { "type": "back" } }
// 弹出弹窗：target = 弹窗页 id（弹窗在 popups 中独立设计，不放在普通页面上）
{ "clickable": true, "clickAction": { "type": "popup", "target": "pp.confirm" } }
```

内容型 Tab——每个页签一棵独立子树：

```jsonc
{ "id": "n9", "type": "tab", "name": "背包分区",
  "rect": [67, 200, 1200, 480],
  "props": { "barPosition": "top", "tabs": ["装备", "消耗品", "材料"] },
  "activeTab": 0,
  "pages": [ { "children": [ /* 页签 1 子控件 */ ] },
             { "children": [ /* 页签 2 子控件 */ ] },
             { "children": [ /* 页签 3 子控件 */ ] } ] }
```

导航型 Tab（定制控件"主页底部导航"）——定义中页签绑定 `goto`，`activeTab` 为暴露属性、按页覆盖：

```jsonc
// 定义 w.home_nav 中：tabs: [ { "label": "商城", "goto": "p.shop" },
//                            { "label": "卡牌", "goto": "p.cards" }, … ]
// 页面 p.cards 上的实例：
{ "id": "n10", "type": "w.home_nav", "name": "底部导航",
  "rect": [0, 1236, 1170, 180], "anchor": { "preset": "bottom-stretch" },
  "props": { "activeTab": 1 } }
```

定制控件实例通过 `slots` 挂实例级子控件（见 §5.5）：

```jsonc
{ "id": "n11", "type": "w.activity_dialog", "rect": [217, 150, 900, 450],
  "props": { "title": "签到奖励" },
  "slots": { "content": [ /* 该实例挂入插槽的子控件 */ ] } }
```

筛选器 + 被过滤的网格项：

```jsonc
{ "id": "n12", "type": "filter",
  "props": { "mode": "single", "options": ["全部", "英雄", "部队", "建筑"], "selected": "全部" },
  "binding": { "target": "n13", "tagKey": "kind" } },
{ "id": "n13-1", "type": "grid-item", "tags": { "kind": "英雄" }, "children": [ /* 卡牌占位 */ ] }
```

## 10. 技术方案

- **框架**：Electron + TypeScript + Vite（electron-vite 脚手架）；前端 React + Zustand（可替换）；
- **渲染**：SVG。每个控件一个 `<g data-id>` 节点：命中测试靠 DOM 事件冒泡、矢量缩放不糊、导出 PNG 方便；选择框 / 手柄画在独立覆盖层；
- **数据**：文档为不可变（immutable）数据结构，所有修改走命令（Command）→ 天然支持撤销重做与"定义更新→实例同步"；
- **导出器插件化**：统一接口 `Exporter { id, name, export(project, options): Promise<输出文件集> }`，内置三种：
  1. **PNG**：SVG 光栅化，支持单页 / 全部页面导出；
  2. **JSON**：直接序列化工程数据（带 schema 版本号，向后兼容）；
  3. **LayaAir**：生成 LayaAir 引擎可用的 UI 文件 / 脚本（首选对接引擎；其它引擎以同样插件方式后续扩展）；
- **预览**：同一套 SVG 渲染器跑在只读 + 交互模式（独立窗口 / 导出 HTML 均可复用）。

## 11. 里程碑

| 阶段 | 内容 | 验收标志 |
|---|---|---|
| M0 骨架 | Electron 工程搭建、画布显示、矩形控件拖放 / 移动 / 缩放、工程保存 / 打开 | 能画几个矩形并存盘 |
| M1 绘图 MVP | 内置控件全集、属性面板、页面管理、图层树、对齐吸附、撤销重做、导出 PNG | 能独立画出一屏完整线框 |
| M2 控件系统 | 定制控件创建 / 发布 / 实例化 / 暴露属性 / 插槽 / 同步更新、锚点与分辨率预览 | 官方模板全部可用，改定义全局生效 |
| M3 交互原型 | 热点、页面跳转、预览窗口、HTML 导出 | 能完整演示一个 UI 流程 |
| M4 导出闭环 | JSON schema 固化、LayaAir 导出器（具体形态后议，见 §12） | 原型文件可直接进引擎继续开发 |

## 12. 决议与开放问题

已决议（2026-08-29）：

1. **横竖屏**：一个工程锁定一种方向，不做横竖双方案（已并入 §6）；
2. **线框风格**：纯白底黑线，不做灰阶 / 主题色块（已并入 §1）；
3. **界面语言**：仅中文，英文后续视需要再加；
4. **Tab 页签栏方向**：只做上 / 下，不做左 / 右（已并入 §4.1）；
5. **TabView 方案**：内置容器 + 两条复用路径（"存为定制控件" / "以容器为骨架新建"）；页签分内容型 / 导航型（`goto`），`activeTab` 可暴露按页覆盖（已并入 §4.1）；
6. **筛选器**：定为内置交互图元，"标记 + 绑定过滤"方案，首期只做单选（已并入 §4.2）；
7. **公共层**：工程级共享层（典型：顶部状态栏 / 常驻导航在所有页面复用）；渲染于页面内容之下、页面可覆盖；普通页面中只读，切到页面列表顶部的"公共层"条目编辑；页面缩略图与 PNG 导出包含公共层（已并入 §2 术语、§9 数据）。

暂缓（当前版本不设计、不排期）：

7. **定制控件共享**：单控件导入导出与团队控件库同步（§5.3 已同步标注）；
8. **LayaAir 导出形态**：引擎场景文件 vs 代码脚本——到 M4 前再结合实际项目工作流决定。

待讨论：暂无。

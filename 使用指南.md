# 手游 UI 雏形设计工具使用指南

> 适用版本：当前开发版（M0–M2 完成，M3 已提供点击交互、编辑器内原型预览、轻提示与最近打开）
> 用途：快速搭建手游界面的线框图、复用界面模块，并演示页面跳转与弹窗流程。

## 1. 先了解工具能做什么

本工具专注于**低保真手游 UI**：用白底黑线的 SVG 控件表达布局、层级和交互流程，而不是制作最终视觉稿。

当前可以完成：

- 建立横屏或竖屏工程，设计多个页面并保存为 `.uiw`；
- 绘制常见 UI 控件，拖放进面板、滚动区和 Tab 页签等容器；
- 用公共层制作所有页面共用的顶部资源栏、底部导航等内容；
- 建立定制控件，在多处复用并让修改同步到全部实例；
- 设置锚点，检查不同手机比例下的布局；
- 配置跳页、返回、弹窗与悬停轻提示，并在内置预览中演示；
- 导出当前设计页为 2 倍 PNG。

暂未提供 HTML 导出、JSON/LayaAir 导出、独立预览窗口、热点和筛选器的预览过滤等能力。

## 2. 创建或打开工程

启动后会进入欢迎页。

1. 点击“新建工程”。
2. 选择横屏或竖屏，并选择合适的设计尺寸预设。
3. 点击“创建”。每个工程固定一种屏幕方向；如需另一种方向，请新建工程。

也可以点击“打开工程…”选择已有 `.uiw` 文件。欢迎页和“文件”菜单均会显示最近打开的工程；失效文件会自动清理，也可手动“清除记录”。

创建后建议立即通过“文件 → 保存”命名并保存工程。工具栏中工程名旁的小圆点表示尚有未保存的更改。

## 3. 认识工作区

界面由以下区域组成：

| 区域 | 作用 |
| --- | --- |
| 顶部菜单与工具栏 | 新建、保存、撤销、对齐、缩放、预览、导出等全局操作。 |
| 左上：控件库 | 内置控件与定制控件。可拖入画布，或单击添加到画布中央。 |
| 左下：页面 | 管理公共层、普通页面、弹窗页和轻提示页。 |
| 中央：画布 | 编辑当前目标；可选中、移动、缩放和拖放控件。 |
| 右侧：属性面板 | 设置选中控件的位置、尺寸、外观、交互、锚点等属性。 |
| 左下（页面下方）：图层 | 查看当前目标的层级，调整显隐、锁定与前后顺序。 |
| 底部状态栏 | 显示编辑目标、设计尺寸、坐标、缩放、吸附状态及分辨率预览入口。 |

## 4. 绘制一张页面

### 添加控件

从左侧控件库将控件拖到画布，或直接单击控件库条目。内置控件分为四类：

- 形状：矩形、圆角矩形、椭圆、线段、占位图、九宫格图；
- 文字：文本；
- 交互：按钮、复选框、进度条、输入框、筛选器；
- 容器：面板、滚动区、列表、网格、Tab 页签。

拖入**容器的内容区域**时，控件会自动成为该容器的子控件。可嵌套多个容器；移动、复制或删除父容器时，子控件会随之处理。容器内容超出可视区域时会被裁剪。

普通页面、公共层和定制控件定义中不能放置“弹窗”或“轻提示框”本体；请分别在专门的弹窗页、轻提示页中设计它们。

### 选择、移动和缩放

- 单击选择控件；按住 `Shift` 单击可增减多选。
- 拖动控件可移动；选框八个方向的手柄可调整大小。
- 方向键每次微调 1 像素，`Shift + 方向键` 每次移动 10 像素。
- 在工具栏打开“网格”和“吸附”，让控件按网格对齐。
- 多选两个或更多控件后，可使用工具栏或右侧属性面板中的对齐功能；等距分布至少需要三个控件。

图层面板可用于选中控件、双击重命名、切换显示/锁定，以及上移、下移、置顶和置底。

### 调整属性

选中单个控件后，右侧属性面板可修改位置与尺寸，并显示该控件可用的属性。例如：

- 文本可设置内容、字号与对齐；
- 按钮可设置文字；
- 进度条可设置进度；
- 列表/网格可设置数量、方向或行列；
- 筛选器可设置选项、当前选中项，以及要关联的列表/网格和标记键；
- 轻提示框可设置气泡尾箭头方向；
- 弹窗可设置标题。弹窗页的根级弹窗改标题时，会同时改名该弹窗页。

## 5. 管理页面、公共层、弹窗与轻提示

### 普通页面

左侧“页面”区域中，点击“＋ 新建页面”创建页面。每个页面行可：

- 单击切换编辑；
- 单击 ✎ 或双击名称重命名；
- 点击 ⧉ 复制；
- 点击 ✕ 删除（工程至少保留一个页面）。

页面缩略图会包含公共层内容。

### 公共层

页面列表最上方的“⚙ 公共层（所有页面共享）”用于放置持续存在的 UI，例如资源栏与底部导航。

公共层绘制在普通页面内容的下方，因此页面控件可以覆盖它。进入普通页面时公共层只读、不可选；如需修改，请回到“公共层”条目。

### 弹窗页

点击“＋ 新建弹窗”会建立一个自带居中弹窗本体的独立弹窗页。弹窗在页面列表“▣ 弹窗”分组中管理，可重命名或删除。

弹窗页中的根级弹窗本体不可删除；请保留它并在其中放置标题、文字、确认按钮等内容。普通控件的点击效果选择“弹出”后，再从下拉列表选择目标弹窗页即可。预览中可以点击标题栏 ✕、遮罩或 `Esc` 关闭弹窗。

### 轻提示页

点击“＋ 新建轻提示”会建立一个带气泡本体的独立轻提示页。可在此设计说明文字、图标等内容；气泡尾箭头方向可在属性面板设置。

回到需要触发提示的控件，在属性面板“轻提示”区开启标记并选择目标轻提示页。预览中鼠标悬停控件会显示提示，移开即关闭。工具会优先将提示显示在目标上方，空间不足时自动翻到下方。

## 6. 使用容器

### Tab 页签

Tab 是多页内容或底部导航的核心容器。

1. 添加 Tab 后，在属性面板修改页签名称、增加或删除页签。
2. 选择页签栏在上方或下方，设置栏高和页签字号。
3. 在画布上点击页签头，切换当前正在编辑的页签内容。
4. 把控件拖到内容区，即放进当前页签。

选中 Tab 后还可拖动页签栏与内容区之间的分界手柄，直接调整栏高。预览中点击页签头会切换其内容。

### 滚动区、列表和网格

滚动区适合放置超过可视高度的内容。原型预览中可直接用鼠标滚轮滚动；嵌套滚动区到达顶部或底部后，滚轮事件会继续传给外层。

列表和网格用于表达重复项。它们当前显示的是统一的占位项，可通过属性设置数量、方向或行列数。筛选器可对同页的列表或网格做标记关联；当前编辑器可配置这一关系，但原型预览中的实际筛选尚未开放。

## 7. 定制控件：一次设计，多处复用

定制控件适合技能栏、商城卡片、资源栏、导航栏等会重复出现的模块。

### 创建方式

可从“控件”菜单选择以下入口：

- “新建定制控件（空白）”；
- 以 Tab、面板或滚动区为骨架新建；
- 在画布中选中一个或多个控件后，选择“存为定制控件”。这会把所选内容保存为定义，并在原位置替换成一个实例。

进入定义编辑后，画布顶部和状态栏会明确提示当前正在编辑的定制控件。修改定义会同步更新工程中所有实例。

### 发布设置与暴露属性

在没有选中内部控件时，右侧属性面板可设置定制控件名称、分组、默认尺寸与建议锚点。

选中定义内部的控件后，可把其可绑定属性（如文本、字号、进度、标题或 Tab 当前页签）绑定为“暴露属性”。之后选中页面上的定制控件实例，就能在右侧填写这些暴露属性；未暴露的内部结构不能在实例中直接修改。

### 插槽与打散

面板、弹窗和滚动区可在定义中标记为“开放为插槽”；Tab 的每个页签默认就是一个插槽。使用实例时，将控件拖到插槽区域，即可为该实例添加独有内容。

若某个实例需要独立修改内部结构，选中它后使用“控件 → 打散实例为普通组合”。打散会解除与定义的同步关系；此操作不会影响其它实例。

在控件库中可双击定制控件名称重命名，点击 ✎ 编辑定义，点击 ✕ 删除定义。删除前请确认其被引用情况；已删除定义的实例会显示为占位。

## 8. 配置交互原型

### 点击效果

按钮默认可点击。其他普通控件需要先在属性面板“点击”区开启“可点击”。然后选择一种效果：

- **切换页面**：选择一个普通页面；
- **返回上一页**：回到当前页面的跳转来源；没有来源时不生效；
- **弹出弹窗**：选择一个已创建的弹窗页。

设置完成后，可用属性面板“▶ 演示点击效果”，或右键选中的控件并选择“点击”来快速验证。

对于定制控件，点击标记必须在**定义内部的具体控件**上配置，实例自身不显示点击设置。这样同一个定义的所有实例都会在对应区域响应点击。

### 运行预览

点击工具栏“▶ 预览”或“视图 → ▶ 运行原型预览”，会从当前普通页面开始进入全屏预览。预览中可：

- 点击配置过效果的控件，切页、返回或打开弹窗；
- 操作弹窗中的可点击控件；
- 点击 Tab 页签切换内容；
- 滚动滚动区；
- 悬停带轻提示标记的控件；
- 使用公共层中的交互控件。

按 `Esc` 或点击右上角关闭按钮退出预览；退出后编辑器会停留在预览的最后一个普通页面。

## 9. 适配不同手机比例

选中控件后，在属性面板“锚点 / 适配”区选择九宫格锚点，并选择尺寸模式：

- **固定**：控件保持原尺寸；
- **随父拉伸**：控件随父级区域伸缩；
- **等比**：控件随父级按比例缩放。

随后通过底部状态栏的“预览”下拉框，或“视图 → 分辨率预览”，切换设计尺寸、16:9、18:9、19.5:9、4:3 等比例。该模式会按锚点重排，但为只读状态；切回“设计尺寸”后继续编辑。

可在“视图”菜单或状态栏打开“安全区”参考框，检查刘海和系统区域的留白。

## 10. 保存、导出与快捷键

工程文件为 `.uiw`（当前版本为 JSON 明文），可从“文件”菜单保存、另存为、打开或关闭工程。切换工程或关闭工程前，若有未保存修改，工具会请求确认。

点击工具栏“导出 PNG”或“文件 → 导出当前页 PNG…”可导出当前编辑页的 2 倍白底 PNG。普通页面的导出会包含公共层。

在 Windows 使用 `Ctrl`，在 macOS 使用 `⌘`：

| 快捷键 | 操作 |
| --- | --- |
| `Ctrl/⌘ + N`、`O`、`S` | 新建、打开、保存。 |
| `Ctrl/⌘ + Z`、`Y` | 撤销、重做。 |
| `Ctrl/⌘ + C`、`V`、`D` | 复制、粘贴、再制。 |
| `Ctrl/⌘ + A` | 全选当前编辑目标内的控件。 |
| `Delete` / `Backspace` | 删除选中控件。 |
| `Ctrl/⌘ + =`、`-` | 放大、缩小画布。 |
| `Esc` | 取消选择、关闭菜单、弹窗演示、轻提示演示或退出预览。 |

## 11. 推荐工作流

以“有顶部资源栏、底部导航、多个业务页面和确认弹窗”的手游界面为例：

1. 新建工程并选择目标屏幕方向和设计尺寸。
2. 进入公共层，搭建顶部资源栏与底部导航；可将复杂导航保存为定制控件。
3. 新建业务页面，逐页从控件库拖入布局；把重复卡片、条目等存为定制控件。
4. 为主要控件设置锚点，并逐个切换常见比例检查布局。
5. 新建弹窗页，设计确认弹窗；为“购买”“返回”等控件配置点击效果。
6. 为术语或图标添加轻提示页并绑定悬停标记。
7. 使用预览从当前页走一遍核心流程，确认跳转、返回、弹窗和滚动行为。
8. 保存 `.uiw`，需要评审图时导出当前页 PNG。

## 12. 当前限制与使用建议

- 粘贴的控件会放在当前编辑目标的根层，不能直接粘贴进特定容器、页签或插槽；需要时请拖动到目标区域。
- 列表和网格的单项是生成的占位结构，不能为每一项单独放置不同子控件；复杂重复内容建议手动摆放或做成定制控件实例。
- 分辨率预览仅用于检查，不能直接编辑或选择控件。
- 定制控件实例缩放时，内部定义会整体拉伸；插槽内的实例内容按页面绝对坐标保存，不随实例缩放。
- 筛选器目前可完成标记与绑定设置，但预览中尚未执行过滤。
- 目前没有自动保存、历史版本、独立预览窗口和 HTML/引擎代码导出。请在重要修改后主动保存。

---

相关资料：功能开发状态见 [开发进度.md](开发进度.md)，产品设计与术语说明见 [概念设计.md](概念设计.md)。

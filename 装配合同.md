# 装配合同

装配合同是 UI 视觉迭代的 **P0 约束**，优先级高于通用素材质量标准和专用美术风格标准。

固定顺序：

1. 装配合同：规定结构、尺寸、层级、状态、复用和运行时行为。
2. 通用素材质量标准：规定透明通道、完整轮廓、安全边、抗锯齿等质量要求。
3. 专用美术风格标准：规定颜色、材质、光影和允许的装饰语言。

风格迭代只能改变第三层；不得重新解释或覆盖前两层。

## 适用范围

以下工作开始前必须读取并执行本合同：

- 基于现有 `.uiw`、页面截图或已确认版本生成风格图、视觉锚点图、组件源板。
- 替换页面皮肤、卡框、按钮、页签、资源条、图标或背景。
- 从风格图提取、裁切、重绘、修图并装配运行时素材。
- 生成 H5、引擎预制体、资产绑定或新的 `.uiw` 迭代版本。

纯粹探索、且明确不对应任何既有页面结构的概念图，可以不受具体页面坐标约束；一旦它被选为某个页面的风格方向，必须先按该页面装配合同重绘为合规风格图，才能进入素材生产。

## 合同事实源

1. 用户最后确认通过的 `.uiw` 是页面结构、尺寸、层级、容器语义和交互的事实源。
2. 用户最后确认通过的真实页面截图只用于核对视觉结果，不得反向改变 `.uiw` 结构。
3. 已确认的组件尺寸、透明窗口、独立图层、状态关系和复用关系属于合同的一部分。
4. 风格图和 ImageGen 输出不是结构事实源；它们只能提供颜色、材质、光影和合同允许的装饰。
5. 若不存在已确认的装配合同，先从权威 `.uiw` 和确认截图中提取并确认合同；不得直接开始风格图或素材生成。

## 不可变内容

除非用户明确要求修改布局或交互，风格迭代必须锁定：

- 设计画布尺寸、方向和页面可视范围。
- 节点的 `x / y / w / h`、父子关系、数组顺序和裁剪边界。
- `tab`、`scroll`、`panel`、`dialog`、custom widget 等容器语义。
- 动态文字、数值、进度、罗马数字和业务状态的运行时节点。
- 图标、底板、边框、立绘、文字和状态高光的独立图层关系。
- 卡牌立绘窗口、等级区、进度区、种族角标等内容插槽的位置与尺寸。
- 组件的目标画布尺寸、对齐基准、锚点、允许伸缩方式和透明区域。
- 资产复用关系；同一皮肤不能因消费者不同而复制为近似素材。
- 背景的运行时方式；纯色、平铺、九宫格或局部容器不得擅自改成整页大图或拉伸纹理。

## 状态与复用合同

- 默认、选中、按下、禁用等状态必须由同一基础轮廓派生。
- 未经合同明确允许，状态变化只能改变颜色、亮度、材质参数和高光；不得改变外轮廓、内边距、结构比例或装饰位置。
- 状态素材的画布尺寸、透明窗口和非透明轮廓必须一致；不能用另一类组件代替选中态。
- 相同 `familyId` 的组件必须共享同一结构模板。资源条、筛选按钮、分页按钮、导航按钮等不同 family 不得交叉代用。
- 图标必须作为独立透明素材叠加；合同已存在独立图标节点时，底板不得再次烘焙图标、图标底座或同义装饰。

## 风格图生成门禁

生成风格图前必须先建立本次任务的合同清单，至少记录：

- 权威 `.uiw` 文件与已确认版本。
- 设计画布尺寸。
- 需要表现的组件 family 及其准确尺寸、比例和消费者。
- 各组件的内容插槽、透明窗口和独立图层。
- 默认态与其他状态的派生关系。
- 背景、滚动区和容器的运行时方式。

风格图必须满足：

1. 页面型风格图使用合同规定的画布、布局和层级，不增删、移动或重构控件。
2. 组件型风格图中，每个组件使用合同规定的比例、结构、透明窗口和状态关系；组件之间保持足够间距，不得互相遮挡。
3. 文本、数值、图标和业务内容按照合同分层；禁止为了画面完整而烘焙进底板。
4. 装饰只能落在合同允许的装饰区，不能占用内容插槽、改变锚点或伸出画布后依赖裁切。
5. 风格图通过装配合同审查后，才能成为素材生产的视觉锚点。仅有“风格接近”但结构不合规的图，只能作为材质参考。

## 生产与验收门禁

- 更换风格图后必须重新测量来源区域；禁止沿用另一张源板的固定裁切坐标。
- 运行时素材必须按合同尺寸导出，并通过通用素材质量门禁。
- 默认态与选中态必须比较画布、透明窗口和轮廓一致性。
- 必须在真实 `.uiw` 渲染器中检查完整页面及所有状态；结构校验或单张素材检查不能替代真实装配预览。
- 真实预览未生成、未检查或存在裁切、错层、状态跳形、背景异常时，不得交付。

## 验收基线封版

用户明确确认页面效果后，必须在同一次封版中完成以下事项，缺一不可：

1. 将确认版本登记为页面合同的 `approvedBaseline`，记录 UIW、真实预览、质量门禁及其 SHA-256。
2. 将本轮确认产生的新增结构限制写入机器可读的 `assemblyGates`，不得只保留在聊天、截图或脚本注释中。
3. 装配脚本必须能从结构源重新生成候选，并与已确认 UIW 做字节级或规范化结果比对。
4. 使用页面合同声明的单一发布命令，依次执行合同校验、可复现装配、UIW 校验、真实渲染验收和哈希核对；任一步失败即整体失败。
5. 已确认 UIW、真实预览、质量门禁、页面合同、专用风格标准、运行时素材和验证脚本必须纳入版本控制；不得只存在于本机未跟踪文件中。

封版后，下一轮视觉迭代必须以 `approvedBaseline.uiw` 作为当前事实源；更早版本只保留为结构来源或历史记录，不能重新覆盖当前确认结果。

以下任一情况直接判定失败：

- 风格图改变组件结构或页面布局。
- 状态切换时按钮轮廓、比例或位置发生变化。
- 卡框、立绘、图标、文字或进度层被错误合并。
- 素材依靠页面裁切隐藏不完整边缘。
- 小纹理在不支持平铺的渲染器中被拉伸为整页背景。
- 使用未经装配合同审查的风格图直接切图或装配。
